SELECT 
    c.CuentaID,
    c.NumeroCuenta,
    c.Saldo,
    t.TransaccionID,
    t.Fecha,
    t.Típo,
    t.Monto,
    t.EmpleadoID
FROM Cuentas c
INNER JOIN Transacciones t
    ON c.CuentaID = t.CuentaID;

