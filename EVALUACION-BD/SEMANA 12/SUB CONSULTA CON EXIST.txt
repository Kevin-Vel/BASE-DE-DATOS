Este comando verifica que si existe sl menos una fila de los redultados la imprime
SELECT nom_cli
FROM Cliente c
WHERE EXISTS (
    SELECT 1
    FROM Prestamos p
    WHERE p.ClienteID = c.ClienteID
      AND p.Estado = 'Activo'
);
