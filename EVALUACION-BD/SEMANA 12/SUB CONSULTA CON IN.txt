Verifica un valor dentro de una conjunto de resultados
mayormente en pequeñas listas:

SELECT nom_cli
FROM Cliente
WHERE ClienteID IN (
    SELECT ClienteID
    FROM Prestamos
    WHERE Estado = 'Activo'
);

