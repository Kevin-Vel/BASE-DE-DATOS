Verifica un valor dentro de una conjunto de resultados
que se encuentra en la lista esto mayormente se 
utiliza en pequeñas listas:

SELECT nom_cli
FROM Cliente
WHERE ClienteID IN (
    SELECT ClienteID
    FROM Prestamos
    WHERE Estado = 'Activo'
);


